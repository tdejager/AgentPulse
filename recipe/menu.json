{
  "$schema": "https://schemas.conda.org/menuinst/menuinst-1-1-0.schema.json",
  "menu_name": "AgentPulse",
  "menu_items": [
    {
      "name": "AgentPulse",
      "description": "Monitor Claude Code agent sessions and get notified when they need input",
      "icon": "{{ MENU_DIR }}/agentpulse.{{ ICON_EXT }}",
      "command": [""],
      "platforms": {
        "osx": {
          "command": ["{{ PREFIX }}/lib/agentpulse/AgentPulse.app/Contents/MacOS/AgentPulse"],
          "CFBundleIdentifier": "com.agentpulse.app",
          "CFBundleVersion": "0.1.0"
        }
      }
    }
  ]
}
